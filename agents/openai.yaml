interface:
  display_name: "global-memory"
  short_description: "Text-first global memory for user habits"
  default_prompt: "Use $global-memory to capture, consolidate, and apply my collaboration preferences through direct text files. Prefer Chinese for generated documents and structured writeups unless I explicitly ask for another language."
